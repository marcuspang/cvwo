import type { NextPage } from "next";
import AuthForm from "../components/Auth/AuthForm";

const Home: NextPage = () => {
  return (
    <div>
      <AuthForm />
    </div>
  );
};

export default Home;
